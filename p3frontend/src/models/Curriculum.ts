export class Curriculum {
    curriculumId : number;
    name : string;

    constructor(curriculumId : number, name : string) {
        this.curriculumId = curriculumId;
        this.name = name;
    }
}